import React, { Component } from 'react'

export default class Orders extends Component {
    constructor (props) {
        super(props);

        this.state = {
        
        };
    }

    componentDidMount() {
        
    }

        
    render() {
        return (
            <div>
                
            </div>
        )
    }

    async getOrders() {
        // const response = fetch('/Orders?' + this.props.customerId)
    }
}
